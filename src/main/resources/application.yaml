spring:
  application:
    name: Cimo

cimo:
  provider: anthropic
  work-dir: ${user.dir}
  anthropic:
    api-key: ${ANTHROPIC_API_KEY:}
    model: claude-sonnet-4-20250514
    max-tokens: 4096
  agent:
    max-tool-rounds: 5
  tool:
    bash:
      timeout-seconds: 30
