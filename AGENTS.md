# Cimo 开发代理流程

## 核心原则

- **先规划，后编码**：不急于写代码，先通过对话把 plan.md 的内容迭代丰满
- **留痕留史**：所有关键决策、plan 的完成都要记录时间和 git commit ID
- **按 plan 行动**：只有我（用户）认为 plan 已经足够完善时，才开始根据 plan.md 进行编码工作
- **第一性原理与奥卡姆剃刀**：所有设计必须先回答“这个抽象/文件/流程解决了什么真实问题”；如果不能证明其独立职责、当前必要性和可验证收益，就不引入或应删除，优先选择能完成目标的最简单设计。

## 工作流程

### 阶段一：Plan 迭代（当前阶段）

1. 通过对话讨论需求、架构、技术选型
2. 持续丰富 `plan.md` 的内容
3. 直到我说「可以开始了」才进入编码阶段

### 阶段二：编码执行

1. 根据 `plan.md` 的内容按序实现
2. 每次实现后更新 `plan.md` 的进度

### Plan 完成标记

当 `plan.md` 中的某个 plan 完成时，需要记录：

- 完成时间
- 对应的 git commit ID

格式示例：

```
## 完成记录

| 完成时间 | Plan | Git Commit |
|---------|------|------------|
| 2026-05-09 12:00 | 项目初始化与基础架构搭建 | abc1234 或 "未提交" |
```

## 工程规范

> 随着项目的推进，这里将不断补充技术选型、代码约定等内容。

- **提示词集中维护**：所有 system prompt、工具引导词、后续多 Agent/多 Step 的提示词都统一放在专门的 `prompt` 包中维护；入口层、Agent Loop、Client adapter 不直接内联大段提示词，只引用 `prompt` 包暴露的常量、模板或工厂方法。
- **CLI 优先入口**：当前阶段采用类似 Codex / Claude Code 的 CLI 交互模式，`./gradlew bootRun` 启动后直接进入 `>` 提示符，用户输入自然语言；入口层使用 JLine REPL 接入 Spring 生命周期，不采用 Spring Shell 的命令模式，也不引入 `ask ...` 这类显式命令壳。
